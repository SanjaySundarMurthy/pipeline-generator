# =============================================================================
# Pipeline Specification — Node.js (Express)
# Generated by pipeline-generator v1.0.0
# =============================================================================

project:
  name: my-express-api
  language: node
  version: "20"
  framework: express

stages:
  - lint
  - test
  - security
  - build

lint:
  tools:
    - eslint
    - prettier

test:
  framework: jest
  coverage: true
  min_coverage: 80

security:
  tools:
    - npm-audit

build:
  type: docker
  dockerfile: Dockerfile
  registry: ghcr.io
