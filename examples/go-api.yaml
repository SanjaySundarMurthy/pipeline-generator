# =============================================================================
# Pipeline Specification — Go API
# Generated by pipeline-generator v1.0.0
# =============================================================================

project:
  name: my-go-service
  language: go
  version: "1.22"

stages:
  - lint
  - test
  - security
  - build

lint:
  tools:
    - golangci-lint

test:
  framework: go-test
  coverage: true

security:
  tools:
    - gosec
    - govulncheck

build:
  type: docker
  dockerfile: Dockerfile
  registry: ghcr.io
